# BriefingGate

Builds an attributable operational briefing from two independently hosted notices. It preserves source digests and requires a clear summary, actions, and both source indexes before the owner can publish.

Run `PYTHONUTF8=1 genvm-lint contracts/contract.py` and `python -m pytest -q`.
